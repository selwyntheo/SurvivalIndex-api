# Keep the directory structure but ignore generated JSONL files during development
# These will be generated by the export service and committed by GitHub Actions

# Ignore all JSONL files
*.jsonl

# But keep the README
!README.md
